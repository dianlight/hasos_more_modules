# Runtime dependencies for hasos_more_modules scripts
# Install with: pip install -r requirements.txt

# GitHub API calls (check_releases.py, build_matrix.py)
requests>=2.31.0

# Type annotations backport for Python 3.9 compatibility
# (dataclasses are built-in from 3.7+, but 'from __future__ import annotations'
#  requires no extra package — listed here for clarity)

# Optional: faster JSON parsing (not strictly required)
# orjson>=3.9.0
